/** A curated set of soft/pastel colors senders can pick directly for a
 * scene's accent — independent of the envelope template's own palette, so
 * it looks good regardless of which envelope they picked. Kept mid-light
 * (not near-white) so the peak-card gradient and white text stay readable. */
export const LIGHT_PALETTE: string[] = [
  "#e8a5b0", // blush pink
  "#f2b880", // peach
  "#f0d17a", // buttery yellow
  "#8fd9c4", // mint
  "#8fc4e8", // sky blue
  "#b8a0e0", // lavender
  "#a8c090", // sage
  "#f29a7a", // coral
  "#a0c8e0", // powder blue
  "#e0a0b8", // rose
  "#e0c9a0", // sand
  "#c8a8d8", // lilac
];
